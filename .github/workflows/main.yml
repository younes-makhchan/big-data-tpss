name: lightweightRegression

on:
  pull_request:
    branches:
      - main

jobs:
  lightweightRegression:
    runs-on: ubuntu-latest
    steps:
      - name: Run lightweightRegression
        run: echo "Hello"