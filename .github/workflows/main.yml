name: lightweightRegression

on:
  workflow_dispatch:

jobs:
  run-lightweight-regression:
    name: Run Lightweight Regression
    runs-on: ubuntu-latest

    steps:
      - name: Execute script
        run: |
          echo "This will fail on purpose"
          exit 1
