name: Trigger Jenkins Manually

on:
    workflow_dispatch:
      inputs:
        pr_number:
          description: 'PR number to test'
          required: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Jenkins
        run: |
          echo "Hello"
